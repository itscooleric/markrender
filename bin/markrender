#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd -- "$SCRIPT_DIR/.." && pwd)"
WORK_ROOT="${MARKRENDER_ROOT:-$PWD}"

MARKRENDER_ROOT="$WORK_ROOT" \
  docker compose -f "$REPO_ROOT/compose.yaml" run --rm render "$@"
